# 山禾颐品上线差距与实施方案

核查日期：2026-09-24。代码基线：`feature/v13-frontend-fusion` 的 `131c05d`。
目的：让小程序、Web 运营后台、API 与数据库形成真实业务闭环，并按证据验收上线。
本次为代码与运行环境核查、技术方案整理；未修改业务逻辑，未部署生产。

## 1. 结论与状态口径

当前处于“前后台商品目录代码链路已接好，本地构建通过，数据库运行联调和真机验收未完成”的阶段，尚不具备正式交易上线条件。
文档历史上的“已完成”不能等同于上线完成：有 service、Prisma repository、migration 或单元测试，只证明相应层已实现。
每项按四层分别记录：业务代码 → 接口已接入 → 前后台已联调（要求真实 MySQL）→ 上线已验收（含微信真机/运维等适用条件）。
没有现场验证的数据库、服务器和微信平台状态，统一标记“未验收”，不凭历史聊天认定已完成。

### 2026-09-24 当前模块四层状态

| 模块 | 代码已实现 | 接口已接入 | 前后台已联调 | 上线已验收 |
|---|---|---|---|---|
| 微信/H5 构建 | 输入目录、依赖兼容与页面编译错误已修复 | 微信/H5 输出包可生成 | 本地 H5 构建可打开 | 否：AppID 为空，微信开发者工具/真机/审核均未做 |
| 后台管理员/RBAC/catalog | 登录、会话、RBAC、商品/SKU/分类写读已有 | 管理 Web 页面调用 HTTP；API 已装配 | 否：无 MySQL；本机只读预览使用演示数据 | 否：真实数据库管理员与权限流程未验收 |
| 首页/分类/商品详情 | 小程序目录/详情/媒体映射已实现 | 读取公开商品、搜索、详情 HTTP；商品 UI 调用接口 | 否：API 3200 与 MySQL 未运行，价格/图片/上下架同步未做真实 E2E | 否：OSS 域名、微信真机与内容素材未验收 |
| 甄选/故事/溯源/首页内容 | 旧 UI/预览已识别，本轮已隐藏样例、展示待接入状态 | 未接正式内容/批次接口 | 否 | 否 |
| 登录/地址/购物车/结算/订单/物流/售后 | 部分领域服务存在 | 消费者 HTTP 与页面业务流未接通 | 否 | 否 |
| 库存履约/供应链/报表 | 有的仅有领域服务；详见下表 | 管理 API/页面不完整 | 否 | 否 |

`npm run check` 本轮通过：33 个测试文件 / 109 项测试。微信/H5/后台构建通过只证明可以构建；不代表数据库联调、微信真机或正式业务验收通过。

## 2. GitHub 与本机事实

- 正式仓库：`https://github.com/renleijianshi/shanheyipin`；正式目录仍为现有 `daima`。
- 本次 `ls-remote`、`fetch origin` 成功，证明当前读取远端通畅；没有验证写权限或执行 push。
- 本轮开始时远端开发分支：`c8e595c`；本地 HEAD：`18c9fb4`，本地领先 3 个提交、落后 0。当前提交/推送 SHA 以本轮结束记录为准。
- 尚未同步的业务提交：`a5de655`（后台 catalog API）、`131c05d`（本机只读预览登录）。本方案文档提交也需后续同步。
- 远端 main：`d73518a`；本地相对 main 领先 14 个提交、落后 0。main 与开发分支不能混称“GitHub 最新代码”。
- Git 远程配置持久保存，但不是实时双向同步。每次修改需验证、commit、push 后比对远端 SHA；不能用有 origin 代替已同步。
- 发现原有未跟踪文件：`apps/miniapp/preview-products.d.ts`、`.js`、`.js.map`、`prototype/pelican-bike.html`。本次保留，不纳入方案提交；构建整理时确认前三者是否为误生成文件。

## 3. 前台—后台—API 差距表

| 功能 | 当前证据与状态 | 必须补齐的工作 | 验收标准 |
|---|---|---|---|
| 首页主视觉、推荐位、快捷入口 | 首页 hero 文案仍为静态 V19；本轮已移除本地商品数据并接公开 catalog；故事/溯源快捷入口标待接入 | 商品位读取公开商品；首页内容配置/搜索页仍未接入 | 否：数据库未运行；内容跨设备未验收 | 否：内容运营接口、素材域名与微信真机未验收；未恢复首页下方故事模块 |
| 分类、SPU/SKU、详情 | API 已返回商品分类编码、已发布商品/在售 SKU；详情页读真实规格/价格 | 后台管理页/API 写 catalog；小程序公开 HTTP port 与页面接入；媒体对象键可按域名解析 | 代码连接已实现，真实 DB 联调未做 | 否：DB、分类编码实际配置、OSS 域名与微信真机未验收 |
| 图片素材 | 小程序不再加载未经批准的外链，按 `VITE_MEDIA_BASE_URL` 解析对象键并显示诚实占位 | 后台表单可保存对象键；OSS 上传/素材库尚未接 | 否 | 否：需授权素材、上传权限及微信合法域名 |
| 本期/山禾甄选 | 小程序旧本机故事/商品素材已移除，显示服务待接入 | 未接内容频道、关联商品和发布 API | 否 | 否；真实内容未发布 |
| 山野故事 | 已隐藏本地 stories 样例，页面说明服务未接入 | M37 内容管理/公共 API 未实现 | 否 | 否；不恢复用户已移除的首页下方故事模块 |
| 扫码溯源 | 已隐藏本地 traces 与演示扫码结果，服务未接时禁用 | M38 批次关联/码查询/停用未接；后续 M39 一盒一码 | 否 | 否；没有真实批次接口/验收 |
| 微信登录、我的、地址 | 用户服务只在 API 领域层；本轮移除本机地址簿保存，页面显示待接入 | 消费者登录 HTTP、会话和地址页面/API未接 | 否 | 否：真实账号、地址隔离、过期恢复流程未验收 |
| 购物车、结算、订单 | 部分领域服务存在；本轮移除演示购物车/本地总价，交易界面明确未开放 | SKU 购物车、服务端报价、订单页面 HTTP/提交/列表/取消未接 | 否 | 否：库存事务和真实数据库下单未验收 |
| 库存与履约 | M23/M24 有库存事务与流水；订单创建/取消和支付回调仓储未见库存接入 | 下单原子预占、取消/超时释放、支付转待出库、发货实物出库、分批发货 | 最后一件并发下单不超卖；每次变化同事务留流水；重复请求不重复扣减 |
| 支付、退款 | Mock/Disabled 服务与回调规则已有；真实渠道 Deferred | 先完成 Mock 全链路和异常恢复；正式收款前单独开启真实支付阶段 | Mock 验收不能称为真实支付上线；真实渠道未启用时不开放正式收款 |
| 发货、物流、售后、补发 | 领域与仓储代码已有；未装配到当前 HTTP 入口；后台没有对应视图 | 管理 API、列表/详情/操作权限；小程序进度/申请入口；退货质检去向 | 一笔订单能发货、查询物流、申请售后、审核、退款/补发；库存与金额一致 |
| 供应商、采购、到货、质检 M17–M20 | service、repository、migration 已有；现有测试文件清单未见这些模块专属测试 | 专项测试、HTTP 接口、后台单据流、质检到批次入库联动 | 供应商→采购→到货→质检全链路可查；数量校验与重复操作受控 |
| 批次、仓库 M21–M22 | 已有基础库存相关模型；独立管理能力待补 | 仓库/仓位/批次管理、采购与质检来源、成本与有效状态 | 入库来源可追踪；余额可追到批次；不可用仓库不接收操作 |
| 盘点、损耗、包装加工 M25–M29 | 缺完整模块 | 盘点单、损耗单、BOM、加工任务、领料、成品批次 | 散货+包材→成品，数量、成本、损耗与流水可追溯；若首发自包装则为上线必需 |
| 营销、会员 M33–M36 | 未形成业务模块；我的权益为占位 | 优惠券、预售、会员、积分按模块实施 | 与报价/库存/退款联动；未实现功能不可对消费者承诺可用 |
| 企业团购 M40–M42 | 未形成业务模块 | 询价、报价、企业订单与多地址履约 | 报价转订单、权限与收货地址隔离、逐地址配送可追踪 |
| 运营后台与报表 M43 | 独立后台只有概览/商品/分类；概览有 4 项查询；预览账号返回样例 | 订单/履约/售后/供应链/内容/报表/账号权限页面；销售/退款/库存口径与导出权限 | 看板来自真实单据，能定位明细；角色只看到并操作授权数据 |

小程序里的 `pages/admin-preview/index.vue` 是本地联动演示，独立后台是 `apps/admin/web/`；两者目前不是一套完整管理系统。正式管理能力统一进入独立后台，消费者发布包移除测试后台入口。

## 4. 已验证与发布阻塞

- 本轮 `npm run check` 通过：lint、TypeScript、33 个测试文件 / 109 项测试、根 tsc build；uni-app Vue 类型检查通过。
- `npm run build:mp-weixin --workspace @shanheyipin/miniapp`、`npm run build:h5 --workspace @shanheyipin/miniapp`、`npm run build --workspace @shanheyipin/admin` 均通过。
- 修复内容：四个 miniapp CLI 脚本设置正确的 `UNI_INPUT_DIR`；根目录锁定 `estree-walker@2.0.2` 兼容 DCloud CJS compiler；地址页 checkbox 改用 `checkbox-group`。
- `manifest.json` 微信 AppID 仍为空；无微信开发者工具、真机或审核发布验收。
- 本次无 `DATABASE_URL`、根 `.env`；API 3200/MySQL 3306 未监听。H5 页面返回服务不可用属于预期连接错误，不是数据库联调。
- 当前测试包含 fake repository 与 HTTP handler 测试，不足以证明真实 MySQL 事务、迁移、并发和全链路正确。
- 当前 CI 执行根 tsc build，未覆盖后台 Vite 构建与微信构建；未配置 MySQL service 进行数据库集成测试。
- H5 界面已用浏览器打开并点击首页、分类、甄选、购物车、我的和地址状态页；桌面后台检查工作台、商品表与只读预览身份。产品详情的运行数据路径因 API/DB 未运行无法浏览真实商品。
- 干净 `npm ci` 报告 40 项依赖漏洞（5 low、22 moderate、13 high）；本轮未执行依赖升级、漏洞修复或生产服务器核查。

## 5. 技术实现方式

### 5.1 保留现有工程和 V19

沿用 uni-app/Vue 3 + Web Vue 后台 + TypeScript 模块化单体 + Prisma/MySQL；保留已确认的 V19 视觉与交互。
当前实际 API 入口是 Node `http`，不是完整 NestJS 应用。先复用现有 service/repository，将 HTTP 按领域拆分和装配，避免为换框架重写业务。
所有客户端通过同一 API 和数据库获得业务事实；本地 storage 只承担可丢弃缓存/草稿，不作为商品、订单或库存的权威来源。

### 5.2 公共客户端与服务端边界

- 小程序建立统一请求层：API 地址、会话、超时、业务错误、登录过期、加载/空/失败状态；用现有 Port 适配，逐页替换演示 store。
- 管理端使用独立会话与 RBAC，每个读写接口服务端鉴权，增加操作审计；菜单隐藏不能代替鉴权。
- 为消费者登录、地址、购物车、结算、订单、物流、售后与内容补 HTTP 路由；现有 catalog 路由继续复用。
- 媒体持久化对象键，展示层解析允许的 OSS 域名；运营上传走受权限保护的流程。
- API 金额为整数分；下单重新核价；数据库事务和唯一约束处理幂等与竞争。

### 5.3 交易与供应链

在同一事务上下文装配订单、库存预占及流水；不能在创建订单后单独调用库存服务并忽略失败。
明确订单取消与支付成功竞争、部分发货、超时任务重试、退款未知结果及补发失败的恢复机制。
先补仓库/批次可管理能力，再接质检入库、领料加工、成品批次和销售出库；复用既有金额/库存规则。
支付继续 Mock/Disabled，真实微信支付、真实退款与真实发货同步遵守 Deferred。全系统可先验收，但正式交易上线需单独完成真实渠道验收；本轮不索取商户资料。

## 6. 实施顺序与退出条件

以下是集成顺序，每次只交付一个模块/切片，不同时重写多个模块。

| 阶段 | 交付内容 | 退出条件 |
|---|---|---|
| A 可重复运行 | 修复微信/H5 构建；固定 Node/依赖；配置开发测试库、迁移、正式管理员；补 CI 构建 | 新环境可安装/构建；API 连测试库；管理员真实登录；微信包能生成 |
| B 第一条前后台闭环 | 分类→SPU→SKU→媒体→小程序首页/分类/详情；接公共商品接口 | 后台新增/上架/修改，另一设备立即按刷新策略看到一致结果 |
| C 首页完整运营 | 首页配置、甄选、故事 M37 与素材；逐一迁移 V19 可见入口 | 后台可发布/撤回内容；消费者没有演示数据和无效跳转 |
| D 用户与交易 | 登录、地址、购物车、结算、订单页面与 HTTP；仓库/批次和订单库存事务；Mock 支付 | 两账号隔离、并发最后一件、重复提交/回调、取消/超时全部通过 |
| E 后台履约 | 发货、物流、售后、退款、补发；M17–M20 接口/页面和入库联动 | 从采购入库到销售售后的单据和库存流水可核对 |
| F 完整经营 | M25–M29 加工/盘点/损耗；M38–M39 溯源；M33–M36 营销；M40–M42 企业业务；M43 报表 | 每项有前台入口（适用时）、后台操作、API、持久化、权限和验收用例 |
| G 上线验收 | 安全/性能/真机/运维/发布；真实交易阶段在解除 Deferred 后单独接入 | 发布清单全部通过，有备份恢复与回退证据，用户确认正式发布 |

本轮完成 A 的可重复构建和 B 的代码层商品目录接入。建议下一步给 A/B 提供开发测试 MySQL，完成“后台商品发布→小程序可见”真实数据库 E2E；没有这项运行条件，任何本地预览都不能作为同步验收。
完整项目仍包含 F，未擅自删掉营销、供应链或企业业务。如果以后决定分期首发，要明确首发包含哪些功能，并将其余入口隐藏或标明未开放，不能将其记为完成。
现在不能依据代码文件数给出完成百分比或可靠总工期；完成 A/B 后按实际交付速度估算剩余工作。

## 7. 上线验收清单

- [ ] 全部公开页面可用，保持 V19 视觉，真机验证滚动、返回、详情购买栏、图片、键盘和弱网。
- [ ] 首页/分类/详情/内容数据来自后台，发布/撤回跨设备一致；演示数据与管理测试入口不进入生产。
- [ ] 登录、地址、购物车、订单、物流、售后全部使用真实接口；越权访问被拒绝。
- [ ] MySQL 干净建库与已有库升级通过；迁移备份和恢复经过演练。
- [ ] 真实数据库验证库存竞争、幂等、事务失败回滚、支付与超时竞争；订单、库存、退款金额可对账。
- [ ] 支付 Deferred 解除前不宣称正式收款可用；正式交易上线完成渠道支付/退款及相关履约验收。
- [ ] 登录限速、权限隔离、会话失效、敏感字段保护、上传校验、审计、依赖安全检查通过。
- [ ] CI 覆盖根检查、后台、H5、微信构建与 MySQL 集成；当前开发分支也有明确验证结果。
- [ ] 配置正式 AppID、请求/媒体域名和 HTTPS；按微信平台当前控制台核对隐私说明、服务类目及提审要求。
- [ ] 授权商品图片、规格/价格/库存、配送与售后说明齐备；不会把样例批次展示为正式质量证明。
- [ ] Caddy + systemd + 现有 MySQL + 轻量 Redis，API 3200；独立部署目录，不触碰旧 `/opt/shanhe-yipin`。
- [ ] 监控错误率/延迟/下单失败/库存异常；健康检查区分进程存活与数据库可用；有告警、备份、日志与发布回退。
- [ ] 测试环境完整走通购买→发货→售后，再进行微信真机验收与正式发布确认。

## 8. 需要用户提供的资料及时间

现在可以继续写接口、页面、测试和修复构建，不要求一次性提供所有资料。
真实数据库联调时需安全配置开发/测试 MySQL；不在聊天粘贴数据库密码。
微信真机阶段需项目 AppID 与开发者权限；素材阶段需授权实拍图、首发 SKU/规格/价格及配送规则。
部署阶段再核对既有服务器、域名、OSS 和运维访问方式；不覆盖旧服务。
真实支付资料继续 Deferred，本方案不索取。

## 9. 下次继续时的最小读取范围

先读 `docs/PROJECT_STATE.md`、本文件对应阶段、`docs/MODULE_INDEX.md` 对应行；再只读当前模块。
下一切片：安全接通测试数据库、执行迁移和首位测试管理员 bootstrap，然后以真实后台操作验收小程序公开商品目录同步。
商品联调重点入口：`apps/api/src/server.ts`、`modules/admin/admin-http-handler.ts`、`modules/catalog/`、`apps/admin/web/`、`apps/miniapp/src/public-catalog-port.ts`、`public-media-url.ts` 与 `pages/home/index.vue` / `pages/category/index.vue`。
每次完成更新五层状态、验证命令、剩余阻塞及 Git SHA；不用重新扫描全仓库。
