# TASKS

> Codex 每次只执行一项任务。优先读取对应 `tasks/Mxx_*.md`，不要一次读取全部任务卡。

当前下一项：**上线基础 A：修复微信构建入口与依赖兼容，验收微信包可构建**。

随后：配置测试 MySQL → 验收后台 catalog → 小程序首页/分类/详情接同库 API。

完整队列、依赖与退出条件见 `docs/LAUNCH_READINESS_PLAN.md` 第 6 节。历史 M01–M46 已有代码优先复用，每次只执行一个切片；完成后更新状态及验证证据。
