# PROJECT_STATE

更新时间：2026-09-24

## 当前阶段

- 正式目录：`C:\Users\19993\Desktop\山禾颐品\daima`；私有仓库 `shanheyipin`，当前分支 `feature/v13-frontend-fusion`。
- 本轮基线 `18c9fb4`；已提交构建修复 `04c399d` 与商品目录接入 `cd785fb`。推送后已核对 `origin/feature/v13-frontend-fusion` 与本地一致于 `cd785fb`；本状态记录在功能提交推送后单独追加。
- A 运行基础：修复 uni-app 输入目录、微信编译依赖和地址页 checkbox 构建错误。`npm ci`、H5 构建、微信小程序包构建和后台构建通过；AppID 仍为空，不能做真机/平台发布验收。
- B 商品闭环代码：商品公开目录 API 返回分类编码；小程序首页、分类和详情通过统一 HTTP port 读取已发布商品、SKU 价格和规格；媒体对象键使用配置的素材域名解析。后台商品与分类 API/页面已有登录、RBAC 和写操作。
- B 实际联调：代码路径已接通，但此机没有 `.env` / `DATABASE_URL`，API 3200 与 MySQL 3306 未监听；因此前后台共库、价格/图片/上下架同步、真实管理员登录均未做数据库运行验收。当前 H5 明确展示商品服务不可用状态。
- 未完成页面已停止展示本地样例商品/购物车/故事/批次/地址。甄选、故事、溯源、购物车、登录/地址、订单、售后等显示待接入状态；小程序包已移除运营预览页路由。
- 本轮 UI 已用 H5 手机框与桌面运营后台预览交互检查。后台开发预览只读并使用演示数据；已标注“只读预览”，不能据此认定真实后台工作流已验收。
- 真实微信支付与真实退款仍为 Deferred；这只阻塞正式收款/退款，不阻塞其他模块开发和 Mock/Disabled 流程。

## 状态口径

| 切片 | 代码已实现 | 接口已接入 | 前后台已联调 | 上线已验收 |
|---|---|---|---|---|
| 微信/H5 构建入口 | 是 | 不适用 | 本地构建通过 | 否：AppID、微信开发者工具、真机与审核未验收 |
| 管理员登录/RBAC/商品管理 | 是 | 后台页面接入真实 HTTP | 否：没有测试数据库；当前只读演示登录 | 否：MySQL、真实管理员及权限验收缺失 |
| 首页/分类/商品详情 | 是：已接公开商品与 SKU 资料 | 是：GET `/api/v1/products`、商品详情；分类服务可公开读取 | 否：API/MySQL 未运行，无法跨后台验证 | 否：数据库、素材域名及微信真机未验收 |
| 登录/地址/购物车/订单/履约/售后 | 只有部分领域服务 | 消费者 API/页面业务未接通 | 否 | 否 |
| 甄选/故事/首页运营/溯源 | UI 合约或本地旧预览曾存在；本轮改为明确待接入 | 正式内容服务未接通 | 否 | 否 |

## 验证

- `npm run check`：ESLint、TypeScript、33 个测试文件 / 109 项测试、根 tsc build 全部通过。
- `npm run typecheck:uni --workspace @shanheyipin/miniapp`：通过。
- `npm run build:mp-weixin --workspace @shanheyipin/miniapp`：通过并生成 `apps/miniapp/dist/build/mp-weixin`；编译器提示未配置 AppID。
- `npm run build:h5 --workspace @shanheyipin/miniapp`：通过。
- `npm run build --workspace @shanheyipin/admin`：通过。
- 浏览器交互：前台首页/分类/甄选/购物车/我的/地址状态页可打开；商品 API 断开时显示错误和重试，不显示商品样例。后台工作台与商品表可打开；只读预览横幅与本机数据状态清晰。无数据库，不能在浏览器编辑并验证真实商品详情。
- `npm ci` 曾报告 40 项依赖漏洞（5 low、22 moderate、13 high）；尚未运行本轮安全审计或升级修复。

## 当前阻塞

- `.env` 与 `DATABASE_URL` 缺失；MySQL 3306 不可用，API 3200 未监听。需要在本地安全配置开发/测试库后才能迁移、bootstrap 管理员和做端到端验证；不要把密码贴到聊天。
- 小程序 `manifest.json` 的 AppID 为空；还需微信开发者工具、真机、请求域名与平台审核验收。
- 商品实拍授权素材和 OSS 媒体域名配置未确认；图片上传/素材库未实现。
- 真实微信支付与支付宝保持 Deferred。正式收款上线前必须单独接入并验收支付、退款和渠道回调。

## 下一步

1. 安全配置测试 MySQL，迁移并创建真实测试管理员；运行 API，完成商品分类/SPU/SKU→后台发布→小程序刷新的一致性 E2E 与失败/下架用例。
2. 配置授权素材与媒体域名，验证后台更换图片后 H5/微信真机同步；补当前固定前台分类编码映射与实际分类数据验收。
3. 接入首页内容、甄选、故事与媒体管理；对应前台从真实发布内容读取。
4. 按 `docs/LAUNCH_READINESS_PLAN.md` 继续用户登录/地址/购物车/结算/订单，再做订单库存预占、取消释放、发货出库与售后。
5. 完成供应链、批次、仓储、加工、损耗、报表、安全、数据库恢复、CI 和发布验收；支付 Deferred 解除前不开放正式收款。

## 已确认约束

- 模块化单体；金额统一整数分；Real WeChat Pay / Real Alipay = Deferred。
- 新 API 绑定 `127.0.0.1:3200`；生产采用 Caddy + systemd + 现有 MySQL + 轻量 Redis。
- 不覆盖、删除或停止服务器旧 `/opt/shanhe-yipin` 及其服务。
