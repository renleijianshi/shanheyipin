# PROJECT_STATE

更新时间：2026-09-25

## 当前阶段

- 正式目录：`C:\Users\19993\Desktop\山禾颐品\daima`；私有仓库 `shanheyipin`，当前分支 `feature/v13-frontend-fusion`。
- 本轮基线 `18c9fb4`；已提交构建修复 `04c399d` 与商品目录接入 `cd785fb`。推送后已核对 `origin/feature/v13-frontend-fusion` 与本地一致于 `cd785fb`；本状态记录在功能提交推送后单独追加。
- A 运行基础：修复 uni-app 输入目录、微信编译依赖和地址页 checkbox 构建错误。`npm ci`、H5 构建、微信小程序包构建和后台构建通过；AppID 仍为空，不能做真机/平台发布验收。
- B 商品闭环代码：公开商品目录、分类、SKU 价格/规格和媒体对象键已接入同一 API；后台支持分类、SPU/SKU、图片上传/换封面、上/下架、可恢复归档和 CSV。
- B 实际联调：隔离测试 MySQL `127.0.0.1:3307`、API `127.0.0.1:3200`、H5 `127.0.0.1:5173`、后台 `127.0.0.1:5174` 正在本机运行。真实可写测试管理员已登录并通过商品新建、2 个 SKU、测试价、两张图、发布、换价/封面、前台刷新、下架、归档/恢复、API 重启持久化及 CSV 文件检查；只读预览账号在内部测试模式关闭。
- M37 山野志文章切片：已新增持久化故事表、草稿/编辑/发布/撤回服务与 API，后台按 `content.story.read/write` RBAC 显示和执行操作；上传沿用受保护本机媒体 API。小程序故事列表和正文从同一个测试库读取，可展示关联在售商品。实际创建带图测试文章、发布、撤回后前台刷新消失、重新发布；API 重启后后台/前台两条已发布测试文章与封面仍可读取。
- 内容范围：目前只接入文章；视频上传、专题、批次溯源和首页内容管理仍未实现，界面明确标出未接入，不据此宣称整个 M37 目标的所有内容类型完成。
- 首页继续保留 V19 主视觉、快捷入口与底部导航；底部整个“山禾好物”模块与关联请求已删除。测试内容/价格/AI 生成样图有内测标注，不声称授权实拍、认证/检测或真实交易。
- 未完成页面已停止展示本地样例商品/购物车/批次/地址。山野故事已从首页快捷入口进入真实公开文章列表/正文；甄选、溯源、购物车、登录/地址、订单、售后等仍显示待接入状态；小程序包已移除运营预览页路由。
- 本轮实际可写后台与 H5 在 390×844 手机视口完成故事列表、编辑、上传、关联商品、发布/撤回、滚动与刷新检查。后台故事页面的桌面布局尚未核验：浏览器视口调整未生效，仍停留在手机宽度；此前的桌面只读预览不代表该写入工作流通过桌面验收。
- 真实微信支付与真实退款仍为 Deferred；这只阻塞正式收款/退款，不阻塞其他模块开发和 Mock/Disabled 流程。

## 用户确认的内部测试版（2026-09-24）

- 用户已授权先准备可操作的内部测试版：使用隔离的测试 MySQL/API 与可写测试管理员，初始化带测试图片的商品、SKU 和展示内容；正式上线时再通过环境配置切换生产库。
- 本机只读预览不能满足目标。第一批由 Walker 承接，验收要求记录在 `tasks/INTERNAL_TEST_WALKER_HANDOFF.md`：后台商品操作与 CSV 下载、测试数据持久化、前后台商品同步，并移除首页底部整个“山禾好物”模块。
- 测试图、价格、内容需明确属于测试数据，不得伪装实拍、质量认证或真实交易。支付继续 Mock/Disabled，真实支付仍 Deferred。

## 状态口径

| 切片 | 代码已实现 | 接口已接入 | 前后台已联调 | 上线已验收 |
|---|---|---|---|---|
| 微信/H5 构建入口 | 是 | 不适用 | 本地构建通过 | 否：AppID、微信开发者工具、真机与审核未验收 |
| 管理员登录/RBAC/商品管理 | 是：含媒体、归档/恢复、CSV | 是：后台页面与服务端读写/RBAC | 是：隔离 MySQL、可写测试账号、图片/SKU/上下架/归档/CSV/重启持久化 | 否：生产库/域名/素材/权限审计未验收 |
| 首页/分类/商品详情 | 是：已接公开商品、SKU 与媒体 | 是：公开商品、详情和分类 HTTP | 是：H5 与后台共享测试 DB；改价换图刷新同步，下架后列表消失 | 否：微信 AppID、开发者工具与真机未验收 |
| M37 山野志（文章切片） | 是：文章草稿、关联商品、发布/撤回及持久化 | 是：后台 RBAC 管理 API、公开列表/详情 HTTP 与媒体 | 是：真实内测管理员创建/上传/发布/撤回；H5 列表/正文/关联商品刷新与 API 重启持久化已验 | 否：微信真机、AppID、生产域名/授权内容未验；视频/专题仍未接 |
| 登录/地址/购物车/订单/履约/售后 | 只有部分领域服务 | 消费者 API/页面业务未接通 | 否 | 否 |
| 甄选/首页运营/溯源 | 仍为待接入 | 首页配置/批次内容 HTTP 未接 | 否 | 否 |

## 验证

- `npm run check`：ESLint、TypeScript、根构建通过；37 个测试文件、119 项测试通过。
- `npm run typecheck:uni --workspace @shanheyipin/miniapp`：通过。
- `npm run build:mp-weixin --workspace @shanheyipin/miniapp`：通过并生成 `apps/miniapp/dist/build/mp-weixin`；编译器提示未配置 AppID。
- `npm run build:h5 --workspace @shanheyipin/miniapp`：通过。
- `npm run build --workspace @shanheyipin/admin`：通过。
- 浏览器交互：后台可写账号实际操作商品表单、SKU 抽屉和文章编辑/封面上传/关联商品/发布/撤回；390×844 下故事列表和正文排版已查看并滚动。H5 分类/商品详情、故事列表/正文、下架隐藏已实际检查；API 重启后后台与 H5 仍读到两条发布文章。商品 CSV 已从后台下载并检查 5 行及 QA 状态/封面键。首页保留 V19 主视觉、快捷入口与底部导航，且没有“山禾好物”模块。
- `npm ci` 曾报告 40 项依赖漏洞（5 low、22 moderate、13 high）；尚未运行本轮安全审计或升级修复。

## 尚未验收

- 小程序 `manifest.json` 的 AppID 为空；还需微信开发者工具、真机、请求域名与平台审核验收。
- 测试图不是授权实拍素材；生产媒体域名和授权素材待提供。
- 真实微信支付与支付宝保持 Deferred。正式收款上线前必须单独接入并验收支付、退款和渠道回调。

## 下一步

1. 继续 M37：实现并验收视频内容与专题管理；此后接首页内容配置、甄选与素材管理，只连接已发布且有真实依据的内容，不恢复首页底部“山禾好物”模块。
2. 再推进消费者登录/地址/购物车/结算/订单与库存履约/售后，按上线计划逐模块增加真实联调。
3. 完成供应链、批次、仓储、加工、损耗、会员营销、溯源、报表、安全、恢复和发布验收；支付 Deferred 解除前不开放正式收款。

## 已确认约束

- 模块化单体；金额统一整数分；Real WeChat Pay / Real Alipay = Deferred。
- 新 API 绑定 `127.0.0.1:3200`；生产采用 Caddy + systemd + 现有 MySQL + 轻量 Redis。
- 不覆盖、删除或停止服务器旧 `/opt/shanhe-yipin` 及其服务。
