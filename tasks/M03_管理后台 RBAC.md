# M03 管理后台 RBAC

## 目标
实现管理员、角色、权限基础模型与鉴权骨架。

## 只读优先
- `AGENTS.md`
- `docs/03_角色权限与业务流程.md`
- `docs/05_数据库_ER与表设计.md`

## 执行
1. 先 `git status` / 搜索现有实现。
2. 只修改本任务相关文件。
3. 添加必要测试。
4. 运行相关 test/typecheck/lint。
5. `git diff --stat` + `git diff` 检查无关改动。
6. 更新 `docs/PROGRESS.md`。

## 输出
只报告：完成项、修改文件、测试结果、风险/阻塞。
